{
    "template_id": "security_policy",
    "template_project": "cdmc-gov-labs",
    "template_region": "us-central1",
    "fields": [
        {
            "field_id": "platform_deid_method",
            "query_expression": "select `cdmc-gov-labs`.remote_functions.get_masking_rule('$project', '$dataset', '$table','$column')"
        }
    ],
    "included_columns_query": "select distinct SUBSTR(asset_name, INSTR(asset_name, '/', -1)+1) from cdmc-gov-labs.tag_history_logs.data_sensitivity where CONTAINS_SUBSTR(asset_name, '$project/dataset/$dataset/table/$table')",
	"included_tables_uris": "bigquery/project/cdmc-confdata-labs/dataset/hr/*",
    "refresh_mode": "ON_DEMAND",
    "tag_history": true,
    "tag_stream": false
}
